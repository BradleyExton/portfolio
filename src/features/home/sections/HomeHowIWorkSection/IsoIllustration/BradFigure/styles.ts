// Animated prop hooks: the pencil scribbles while writing and the pointing
// arm sweeps while directing. Both ride the iso-sway loop from globals.css,
// so they only run while the scene's stage row is data-active.
export const scribble = "iso-loop-sway [--iso-sway-duration:1.1s]";
export const sway = "iso-loop-sway";
