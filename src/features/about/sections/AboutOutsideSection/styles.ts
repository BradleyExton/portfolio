import { spacing, typeScale } from "@/features/shared/designSystem";

export const section = `${spacing.section} bg-surface-muted`;
export const container = spacing.container6;
export const panel = "rounded-2xl border border-border-default bg-surface p-5 shadow-[0_18px_38px_-30px_color-mix(in_srgb,var(--color-brand-tint)_75%,transparent)] sm:p-6 md:p-8";
export const heading = `${typeScale.sectionTitle} !mb-6`;
export const description = `max-w-4xl ${typeScale.leadBody}`;
